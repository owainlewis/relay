description: A simple GET request
request:
  method: GET
  url: https://httpbin.org/{{param "id"}}
  query:
    foo: bar
    baz: qux
  headers:
    Content-Type: application/json
    Authorization: Bearer {{env "AUTH_TOKEN"}}
